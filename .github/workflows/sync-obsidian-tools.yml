name: Obsidian corpus sync disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Tools content is embedded directly in rickyjreyes.github.io and no longer syncs from rickyjreyes/obsidian."
